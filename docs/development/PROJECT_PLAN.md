# AI 에이전트 기반 콘텐츠 자동화 시스템 개발

## 프로젝트 개요

### 프로젝트 주제
AI 에이전트 기반 콘텐츠 자동화 시스템 개발

### 프로젝트 제작 배경
최근 커머스 업계는 AI를 활용한 콘텐츠 자동 생성 및 마케팅 자동화 기술 도입이 빠르게 확산되고 있습니다.
그러나 대부분의 자동 콘텐츠는 퀄리티가 낮거나, 플랫폼 간 통합이 어려워 수작업이 여전히 필요합니다.
본 프로젝트는 상품 수집 → 콘텐츠 생성 → 자동 배포의 전 과정을 AI 에이전트가 수행함으로써, 트래픽 증가, 운영 효율성, 광고비 절감을 동시에 달성하고자 합니다.

### 프로젝트 달성 목표
1. 트렌드 이슈상품을 추출한다.
2. LLM으로 추출한 이슈상품을 주제로 콘텐츠를 자동 생성한다.
3. 자동생성된 콘텐츠를 블로그 등의 채널에 자동 업로드한다.

## 기능적 요구사항

### ✔ ① 트렌드 데이터 수집 모듈
**목표:** '이슈 상품 50개'를 자동으로 수집하고 정제하는 알고리즘 개발

**필요 기능:**
- Google Trends 크롤링(or unofficial API)
- 관련 검색어 / 관련 주제 확장
- 키워드 필터링 규칙
- 키워드 중복 제거
- Top 30~50개 자동 선정
- 최종적으로 1개 랜덤 선택

### ✔ ② AI 콘텐츠 생성 엔진
**핵심 파트**

#### 2-1) 제목 생성
- SEO 최적화
- CTR 높은 제목 구조 만들기
- 예: "부모들이 미친 듯 찾는 ○○, 왜 갑자기 인기일까?"

#### 2-2) 아웃라인 생성
- 본문 5~7개 섹션 자동 생성

#### 2-3) 본문 생성
- 키워드 기반 1,000~1,500자 블로그 글 생성
- 자연스럽고 광고 같지 않은 문체
- SEO 고려해 h2, h3 구조까지

#### 2-4) 이미지 생성 (선택)
- DALL·E 사용
- 상품 리뷰 느낌 이미지
- (만약 실사 느낌이 필요하면 미드저니 API or 프롬프트 제공)

#### 2-5) 최종 텍스트 포맷팅
- h2, h3 포함된 HTML 형식으로 구성
- 백엔드가 블로그에 넣기 편하게 포맷 제공

### ✔ ③ ssadagu.kr 상품 수집
- 선택된 상품명으로 키워드로 ssadagu.kr 몰에서 상품 제목을 수집
- 수집된 상품 정보를 콘텐츠 생성에 활용

### ✔ ④ 결과 저장
- 생성된 콘텐츠를 DB/파일로 저장
- 백엔드가 업로드할 수 있도록 구조화된 형태로 저장

### ✔ ⑤ AI 스케줄러
**목표:** 매일 자동으로 업로드할 글을 생성하는 로직

- 매일 오전 10시에 실행
- 트렌드 크롤링 + 콘텐츠 생성 → 결과를 DB/파일로 저장
- 백엔드가 매일 자동 업로드를 담당한다면, AI 팀은 "업로드할 글을 매일 자동 생성하는 로직"을 만듦

## 환경 설정 계획

### 1. 프로젝트 구조
```
final_project/
├── venv/                    # 가상환경 (이미 존재)
├── src/                     # 소스 코드
│   ├── __init__.py
│   ├── main.py             # 메인 실행 파일
│   ├── agents/             # AI 에이전트 모듈
│   │   ├── __init__.py
│   │   ├── trend_collector.py    # ① 구글 트렌드 수집 (50개 → 필터링 → Top 30-50)
│   │   ├── product_collector.py  # ③ ssadagu.kr 상품 수집
│   │   └── content_generator.py  # ② AI 콘텐츠 생성 엔진
│   │       ├── title_generator.py      # 2-1) 제목 생성
│   │       ├── outline_generator.py    # 2-2) 아웃라인 생성
│   │       ├── body_generator.py       # 2-3) 본문 생성
│   │       ├── image_generator.py      # 2-4) 이미지 생성 (선택)
│   │       └── formatter.py            # 2-5) 최종 텍스트 포맷팅
│   ├── storage/            # 저장 모듈
│   │   ├── __init__.py
│   │   ├── db_handler.py   # DB 저장 (선택)
│   │   └── file_handler.py # 파일 저장 (JSON/HTML)
│   ├── utils/              # 유틸리티
│   │   ├── __init__.py
│   │   ├── config.py       # 설정 관리
│   │   ├── logger.py       # 로깅
│   │   └── filters.py      # 키워드 필터링 규칙
│   └── scheduler.py        # ⑤ 스케줄러 (매일 10:00 실행)
├── config/                 # 설정 파일
│   └── .env.example        # 환경변수 예시
├── data/                   # 데이터 저장
│   ├── trends/             # 트렌드 데이터
│   ├── products/           # 상품 데이터
│   └── contents/           # 생성된 콘텐츠 (JSON/HTML)
├── logs/                   # 로그 파일
├── requirements.txt        # 패키지 의존성
├── .gitignore
└── README.md
```

### 2. 필요한 라이브러리
- **웹 스크래핑**: 
  - `selenium` - 동적 웹페이지 크롤링
  - `beautifulsoup4` - HTML 파싱
  - `requests` - HTTP 요청
  - `pytrends` - Google Trends API (unofficial)
- **LLM**: 
  - `openai` - GPT 모델 (텍스트 생성, DALL-E 이미지 생성)
  - 또는 `anthropic`, `google-generativeai` (대안)
- **스케줄링**: 
  - `schedule` 또는 `APScheduler` - 정기 실행
- **데이터 저장**: 
  - `sqlite3` 또는 `sqlalchemy` - DB 저장 (선택)
  - `json` - JSON 파일 저장
- **환경변수**: 
  - `python-dotenv` - 환경변수 관리
- **기타**: 
  - `lxml` - XML/HTML 파싱
  - `webdriver-manager` - Selenium 드라이버 관리
  - `pandas` - 데이터 처리 (선택)

### 3. 설정 파일
- `.env`: API 키, 네이버 블로그 인증 정보 등
- `config.py`: 설정 관리

## 진행 상황

### 2025-11-17
- [x] 프로젝트 계획 수립
- [x] 상세 요구사항 정리 및 구조 설계
- [ ] 환경 설정 완료
- [ ] ① 트렌드 데이터 수집 모듈 구현
  - [ ] Google Trends 크롤링
  - [ ] 관련 검색어/주제 확장
  - [ ] 키워드 필터링 및 중복 제거
  - [ ] Top 30-50개 선정 및 랜덤 선택
- [ ] ③ ssadagu.kr 상품 수집 기능 구현
- [ ] ② AI 콘텐츠 생성 엔진 구현
  - [ ] 2-1) 제목 생성 (SEO 최적화)
  - [ ] 2-2) 아웃라인 생성 (5-7개 섹션)
  - [ ] 2-3) 본문 생성 (1,000-1,500자, HTML 구조)
  - [ ] 2-4) 이미지 생성 (DALL-E, 선택)
  - [ ] 2-5) 최종 텍스트 포맷팅
- [ ] ④ 결과 저장 기능 구현 (DB/파일)
- [ ] ⑤ 스케줄러 구현 (매일 10:00 실행)

## 참고사항
- 모든 업데이트 내용은 이 문서에 기록됩니다.
- 각 기능 구현 시 상세 내용은 별도 섹션에 추가됩니다.

